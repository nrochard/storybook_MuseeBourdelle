import { createAnimation } from './Animation';

export default {
  title: 'Components/Animation',
};

const Template = (args) => createAnimation(args);

export const Animation = Template.bind({});
Animation.args = {};
