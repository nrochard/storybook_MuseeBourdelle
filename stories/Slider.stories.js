import { createSlider } from "./Slider";

export default {
  title: "Components/Slider",
};

const Template = (args) => createSlider(args);

export const Slider = Template.bind({});
Slider.args = {};
